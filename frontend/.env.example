# Leave empty for local development (Vite's dev proxy handles /api requests).
# For production deployment, set this to your deployed backend's full URL,
# e.g. https://ragforge-backend.onrender.com
VITE_API_BASE_URL=
